# إضافة صوت إشعار مخصص - بيكيا

## الصوت الحالي

حالياً، التطبيق يستخدم **صوت مولد** باستخدام Web Audio API:
- نغمتين متتاليتين (دينج دونج)
- صوت بسيط وواضح
- يعمل على كل المتصفحات

## كيفية إضافة ملف صوت مخصص

### الخطوة 1: احصل على ملف صوت

يمكنك:
1. **تحميل صوت مجاني** من:
   - https://notificationsounds.com/
   - https://freesound.org/
   - https://mixkit.co/free-sound-effects/notification/

2. **إنشاء صوت خاص** باستخدام:
   - Audacity (مجاني)
   - GarageBand (Mac)
   - أي برنامج تسجيل صوت

### الخطوة 2: تحضير الملف

**المواصفات المثالية:**
- الصيغة: MP3 أو OGG
- المدة: 0.5 - 2 ثانية
- الحجم: أقل من 50KB
- الجودة: 128kbps كافية

### الخطوة 3: إضافة الملف للمشروع

1. ضع الملف في مجلد `public/`
2. سمه `notification.mp3`
3. التطبيق سيستخدمه تلقائياً!

```
bekya/
├── public/
│   ├── notification.mp3  ← ضع الملف هنا
│   └── favicon.ico
├── src/
└── ...
```

### الخطوة 4: اختبر الصوت

1. أعد تشغيل التطبيق
2. انتظر إشعار جديد
3. الصوت سيشتغل تلقائياً!

## أنواع الإشعارات

### 1. صوت الإشعار 🔊
- يشتغل **فوراً** لما يجي إشعار جديد (Realtime)
- صوت قصير (دينج دونج)
- يمكن تغييره بملف MP3
- **لا يتكرر** - يشتغل مرة واحدة فقط مع كل إشعار جديد

### 2. إشعار المتصفح 🔔
- يظهر خارج التطبيق
- يحتاج إذن من المستخدم
- يعمل حتى لو التطبيق مغلق (في بعض المتصفحات)

## التحكم في الصوت

### تغيير مستوى الصوت

في `Navbar.jsx`، غير هذا السطر:

```javascript
audio.volume = 0.5; // من 0.0 (صامت) إلى 1.0 (أعلى صوت)
```

### تعطيل الصوت

احذف أو علق على هذا السطر:

```javascript
// playNotificationSound(); // معطل
```

### تغيير تردد الصوت المولد

في `playGeneratedSound()`:

```javascript
oscillator1.frequency.value = 800; // غير الرقم (400-2000)
oscillator2.frequency.value = 1000; // غير الرقم
```

## أمثلة أصوات مقترحة

### 1. صوت رسالة WhatsApp
- بسيط وواضح
- الجميع يعرفه
- ✅ مناسب

### 2. صوت جرس
- كلاسيكي
- واضح
- ✅ مناسب

### 3. صوت "بينج"
- قصير جداً
- احترافي
- ✅ مناسب

### ❌ تجنب:
- أصوات طويلة (أكثر من 3 ثوان)
- أصوات مزعجة
- أصوات غير واضحة

## استكشاف الأخطاء

### الصوت لا يعمل؟

**السبب 1: المتصفح يمنع الصوت التلقائي**
- الحل: المستخدم لازم يتفاعل مع الصفحة أولاً (أي ضغطة)

**السبب 2: الملف غير موجود**
- تأكد من وجود `notification.mp3` في `public/`
- تأكد من اسم الملف صحيح

**السبب 3: صيغة الملف غير مدعومة**
- استخدم MP3 أو OGG
- تجنب WAV (حجم كبير)

### إشعار المتصفح لا يظهر؟

**السبب: المستخدم لم يسمح بالإشعارات**
- الحل: اطلب الإذن مرة أخرى
- في Chrome: Settings → Privacy → Notifications

## الكود المسؤول

### في Navbar.jsx

```javascript
// تشغيل الصوت
const playNotificationSound = () => {
  const audio = new Audio('/notification.mp3');
  audio.volume = 0.5;
  audio.play().catch(() => {
    playGeneratedSound(); // fallback
  });
};

// إشعار المتصفح
const showBrowserNotification = () => {
  if (Notification.permission === 'granted') {
    new Notification('بيكيا - إشعار جديد', {
      body: 'منتج جديد متاح في منطقتك',
      icon: '/favicon.ico'
    });
  }
};
```

## ملاحظات مهمة

1. **الصوت يشتغل فقط لو المستخدم تفاعل مع الصفحة** (قيد أمان المتصفح)
2. **إشعارات المتصفح تحتاج إذن** من المستخدم
3. **الصوت المولد backup** إذا فشل تشغيل الملف
4. **الإشعارات في الوقت الفعلي** - الصوت يشتغل فوراً مع كل إشعار جديد (بدون تأخير)
5. **لا تكرار** - الصوت يشتغل مرة واحدة فقط لكل إشعار جديد

## تحسينات مستقبلية

- [ ] إعدادات للتحكم في الصوت (تشغيل/إيقاف)
- [ ] اختيار صوت من قائمة
- [ ] تحكم في مستوى الصوت من الإعدادات
- [ ] أصوات مختلفة لأنواع إشعارات مختلفة
