# نظام الإشعارات - بيكيا

## كيف يعمل النظام؟

### 1. متى تُرسل الإشعارات؟

**عند موافقة Admin على منتج:**
- النظام يجيب location البائع
- يبحث عن كل المستخدمين في نفس المنطقة
- يرسل إشعار لكل واحد فيهم

### 2. محتوى الإشعار

```
🆕 منتج جديد متاح في منطقتك: [اسم المنتج]
```

مع:
- صورة المنتج
- السعر النهائي
- وقت الإشعار

### 3. أين تظهر الإشعارات؟

#### أ) في Navbar
- أيقونة جرس 🔔
- Badge أحمر بعدد الإشعارات الجديدة
- مثال: 🔔 **3** (3 إشعارات جديدة)

#### ب) في صفحة الإشعارات
- كل الإشعارات مرتبة من الأحدث للأقدم
- الإشعارات الجديدة بخلفية خضراء فاتحة
- نقطة خضراء على الإشعارات غير المقروءة

### 4. التفاعل مع الإشعارات

**الضغط على إشعار:**
- يتعلم كمقروء تلقائياً
- يروح للصفحة الرئيسية (Home) لمشاهدة المنتج

**زر "تعليم الكل كمقروء":**
- يعلم كل الإشعارات كمقروءة دفعة واحدة

### 5. التحديث التلقائي

- الإشعارات تتحدث **كل 30 ثانية** تلقائياً
- Badge في Navbar يتحدث تلقائياً
- مفيش حاجة للـ refresh

## مثال عملي

### السيناريو:

1. **أحمد** في **المعادي** يبيع كرسي
2. Admin يوافق على الكرسي
3. النظام يبحث عن كل المستخدمين في **المعادي**
4. يلاقي: **سارة، محمد، فاطمة**
5. يرسل إشعار لكل واحد:

```
🆕 منتج جديد متاح في منطقتك: كرسي خشبي
السعر: 300 جنيه
```

6. **سارة** تفتح التطبيق:
   - تشوف Badge أحمر على الجرس: 🔔 **1**
   - تضغط على الجرس
   - تشوف الإشعار بخلفية خضراء
   - تضغط على الإشعار
   - تروح Home وتشوف الكرسي

## قاعدة البيانات

### جدول notifications

```sql
CREATE TABLE notifications (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES profiles(id),
  product_id UUID REFERENCES products(id),
  message TEXT NOT NULL,
  type TEXT NOT NULL,
  read BOOLEAN DEFAULT false,
  created_at TIMESTAMP
);
```

### أنواع الإشعارات (type)

- `new_product`: منتج جديد في منطقتك
- `price_update`: تحديث سعر
- `negotiation`: عرض تفاوض
- `approved`: تم الموافقة على منتجك
- `rejected`: تم رفض منتجك

## الكود المسؤول

### في productStore.js

```javascript
sendLocationNotifications: async (product) => {
  // 1. جيب location البائع
  const { data: seller } = await supabase
    .from('profiles')
    .select('location')
    .eq('id', product.user_id)
    .single();

  // 2. جيب كل المستخدمين في نفس المنطقة
  const { data: nearbyUsers } = await supabase
    .from('profiles')
    .select('id, username')
    .eq('location', seller.location)
    .neq('id', product.user_id); // ما عدا البائع نفسه

  // 3. أرسل إشعار لكل واحد
  const notifications = nearbyUsers.map(user => ({
    user_id: user.id,
    product_id: product.id,
    message: `منتج جديد متاح في منطقتك: ${product.title}`,
    type: 'new_product'
  }));

  await supabase.from('notifications').insert(notifications);
}
```

## ملاحظات مهمة

1. **الإشعارات تُرسل فقط للمستخدمين في نفس المنطقة**
2. **البائع نفسه مش بيستلم إشعار** (منطقي!)
3. **التحديث التلقائي كل 30 ثانية** (ممكن تغيره)
4. **الإشعارات محدودة بـ 50 إشعار** (آخر 50)
5. **الإشعارات القديمة مش بتتمسح** (ممكن تضيف cleanup لاحقاً)

## تحسينات مستقبلية

- [ ] Push Notifications (إشعارات خارج التطبيق)
- [ ] Real-time updates (بدون انتظار 30 ثانية)
- [ ] تصنيف الإشعارات حسب النوع
- [ ] حذف الإشعارات القديمة تلقائياً
- [ ] إشعارات صوتية
