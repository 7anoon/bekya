# بيكيا - منصة بيع المنتجات

منصة ذكية لبيع وشراء المنتجات مع تحليل AI وإعادة تدوير.

## المميزات

- 🛒 بيع وشراء المنتجات بسهولة
- 💰 نظام تسعير ذكي
- 🤝 نظام تفاوض بين البائع والأدمن
- 🔔 إشعارات فورية
- 📱 تصميم متجاوب (Responsive)
- 🎉 نظام عروض وخصومات
- 📦 إدارة كاملة للمنتجات

## التقنيات المستخدمة

- React + Vite
- Supabase (Database & Auth)
- Zustand (State Management)
- React Router
- PWA Support

## التثبيت والتشغيل

```bash
# تثبيت المكتبات
npm install

# تشغيل السيرفر المحلي
npm run dev

# بناء المشروع
npm run build
```

## إعداد Supabase

1. أنشئ حساب على [Supabase](https://supabase.com)
2. أنشئ مشروع جديد
3. نفذ الـ migrations من ملف `COMPLETE_SUPABASE_MIGRATIONS.sql`
4. أنشئ ملف `.env` وأضف:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

## الرفع على GitHub Pages

1. غير `base` في `vite.config.js` لاسم الـ repository
2. أضف Secrets في GitHub:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
3. فعّل GitHub Pages من Settings → Pages → Source: GitHub Actions

## الترخيص

MIT License

- 🤖 تحليل ذكي للمنتجات باستخدام AI
- 💰 تسعير تلقائي بناءً على حالة المنتج (حد أقصى 500 جنيه)
- ♻️ اقتراحات لإعادة التدوير
- 📍 إشعارات للمستخدمين في نفس المنطقة
- 👤 لوحة تحكم للمستخدمين
- 👨‍💼 لوحة إدارة كاملة
- 📱 PWA - يعمل على الموبايل كتطبيق

## التقنيات المستخدمة

- React 18
- Vite
- Supabase (Database + Auth + Storage)
- Zustand (State Management)
- PWA Support

## التثبيت والإعداد

### 1. تثبيت المكتبات

```cmd
npm install
```

### 2. إعداد Supabase

1. إنشاء مشروع جديد على [Supabase](https://supabase.com)
2. تنفيذ السكريبت `supabase-schema.sql` في SQL Editor
3. إنشاء bucket اسمه `products` في Storage

### 3. إعداد المتغيرات البيئية

إنشاء ملف `.env` ونسخ المحتوى من `.env.example`:

```
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_AI_API_KEY=your_ai_api_key
```

### 4. تشغيل المشروع

```cmd
npm run dev
```

### 5. بناء المشروع للإنتاج

```cmd
npm run build
```

## النشر على Netlify

1. رفع المشروع على GitHub
2. ربط المشروع مع Netlify
3. إضافة المتغيرات البيئية في Netlify
4. Build command: `npm run build`
5. Publish directory: `dist`

## إنشاء حساب Admin

بعد التسجيل، قم بتحديث role في جدول profiles:

```sql
UPDATE profiles 
SET role = 'admin' 
WHERE username = 'your_username';
```

## الاستخدام

### للمستخدمين:
1. التسجيل بـ username وإدخال الموقع
2. إضافة منتج مع 4 صور
3. اختيار بيع أو إعادة تدوير
4. انتظار موافقة الإدارة

### للإدارة:
1. مراجعة المنتجات المعلقة
2. الموافقة أو الرفض أو التفاوض
3. عند الموافقة يتم إرسال إشعارات للمستخدمين القريبين

## الهيكل

```
bekya/
├── src/
│   ├── components/      # المكونات
│   ├── pages/          # الصفحات
│   ├── store/          # إدارة الحالة
│   ├── lib/            # المكتبات
│   ├── App.jsx         # التطبيق الرئيسي
│   └── main.jsx        # نقطة الدخول
├── public/             # الملفات الثابتة
└── index.html          # HTML الرئيسي
```

## الملاحظات

- السعر الأقصى للمنتجات: 500 جنيه
- يجب رفع 4 صور بالضبط لكل منتج
- الإشعارات تذهب فقط للمستخدمين في نفس المنطقة
- التسجيل يتم بـ username وليس email

## الدعم

للمشاكل والاستفسارات، يرجى فتح issue على GitHub.
